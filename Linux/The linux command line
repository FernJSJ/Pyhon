# 第二章
date # 显示系统当前时间和日期
cal # 显示当前月份的日历
df # 查看磁盘剩余空间
free # 查看空闲内存
exit # 终止终端
Ctrl + Alt + F1-6 # 幕后控制台
 
# 第三章
pwd # 显示当前工作目录
ls # 列出目录包含的文件及子目录
	ls-a # 列出所有文件
	ls-l # 结果以长模式输出
	ls-t # 按文件修改时间的先后排序
	-r # 相反顺序输出
	-ld # 看到指定目录的详细信息
	-F # 指示符
	-h # 可读模式
	-S # 按文件大小排序
	 
cd # 更改工作目录

# 第四章
file + 文件名 # 打印文件内容的简单描述
less + 文件名 # 浏览文本文件 
	Page UP or b # 向上翻页
	Page Down or space # 向下翻页
	UP Arrow # 向上一行
	Down Arrow # 向下一行
	G # 移动到最后一行
	1G or g # 移动到开头一行
	/charaters # 向前查找指定的字符串
	n # 向前查找下一个出现的字符串(该字符为之前指定查找的)
	h # 帮助
	q # 退出

# 第五章
cp # 复制
mv # 移动
mkdir # 创建目录
rm # 删除
ln # 创建链接

* # 任意多个字符
? # 任意一个字符
[characters] # 任意一个属于字符集的字符
[!characters] # 任意一个不是字符集的字符
[[:class:]] # 任意一个属于指定字符中的字符

# 第六章


	

